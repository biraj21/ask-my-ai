{
  "name": "ask-my-ai",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "pack": "pnpm run build && zip -r releases/ask-my-ai-v$npm_package_version.zip dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.0",
    "@types/chrome": "^0.1.14",
    "@types/node": "^24.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.1.7"
  }
}
